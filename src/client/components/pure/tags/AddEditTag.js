import React from 'react';
import AddEditEntity from '../common/AddEditEntity';

const AddEditTag = ({ user, match }) => <AddEditEntity user={user} match={match} entity="tag" title="тэг" />

export default AddEditTag
