import React from 'react';
import RemoveEntity from '../common/RemoveEntity';

const RemoveTag = ({ user, match }) => <RemoveEntity user={user} match={match} entity="tag" title="тэг" />

export default RemoveTag
