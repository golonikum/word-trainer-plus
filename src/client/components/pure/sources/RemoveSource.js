import React from 'react';
import RemoveEntity from '../common/RemoveEntity';

const RemoveSource = ({ user, match }) => <RemoveEntity user={user} match={match} entity="source" title="источник" />

export default RemoveSource
