import React from 'react';
import AddEditEntity from '../common/AddEditEntity';

const AddEditSource = ({ user, match }) => <AddEditEntity user={user} match={match} entity="source" title="источник" />

export default AddEditSource
